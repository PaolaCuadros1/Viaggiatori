<div class="card my-2">
  <h1 class="card-header"><%= t('activerecord.attributes.admin.editing') %></h1>
  <div class="card-body">
    <%= render 'form', admin: @admin %>
  </div>
</div>

<div class="d-flex justify-content-end">
  <div class="mb-2">
    <%= link_to "<span class='material-icons'>visibility</span>#{t('action.show')}".html_safe, admin_admin_path(@admin), class: 'btn btn-info mr-1' %>
    <%= link_to t('action.back'), admin_admins_path, class: 'btn btn-secondary' %>
  </div>
</div>
